


k=1;
for i = 1:1:7
    for j = 1:1:9
        mean_val(i,j) = mean(newdata{i,j});
      
        mean_val(i+7,j) = mean(down{i+10,j});
  
        mean_val(i+14,j) = mean(right{i+20,j});

        mean_val(i+21,j) = mean(left{k+30,j});

    end
    k=k+1;
end
